apiVersion: v1
kind: ReplicationController
metadata:
  name: my-replicator
spec:
  replicas: 2
  selector:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: my-container
        image: nginx

