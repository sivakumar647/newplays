apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-service
  name: my-service
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30060
  selector:
    app: my-web-service
