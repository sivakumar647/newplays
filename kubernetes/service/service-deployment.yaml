apiVersion: v1
kind: Service
metadata: 
  name: servicess
  labels:
    app: servicess
spec:
  type: NodePort
  ports:
   - port: 80
     protocol: TCP
     targetPort: 80
     nodePort: 30020
  selector:
   app: service-config
