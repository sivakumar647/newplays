apiVersion: v1
kind: Service
metadata:
 name: service
 namespace: alc-prod
 labels:
  app: web-service
spec:
 type: NodePort
 ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30070
 selector:
  app: webs 
